<%= form_for(@author_role) do |f| %>
  <% if @author_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@author_role.errors.count, "error") %> prohibited this author_role from being saved:</h2>

      <ul>
      <% @author_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role %><br />
    <%= f.text_field :role %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
