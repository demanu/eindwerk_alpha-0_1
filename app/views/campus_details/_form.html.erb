<%= form_for(@campus_detail) do |f| %>
  <% if @campus_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campus_detail.errors.count, "error") %> prohibited this campus_detail from being saved:</h2>

      <ul>
      <% @campus_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
